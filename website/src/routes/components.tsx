import { Link } from "react-router";

export default function Components() {
	return (
		<section>
			<h1>Components</h1>
			<p>In the future, this will list all of the components.</p>
			<ol>
				<li>
					<Link to="./loader">Loader</Link>
				</li>
			</ol>
		</section>
	);
}
